import IconFrame from "./ui/IconFrame";

export default function Footer(params) {
  return (
    <footer>
      {/* <IconFrame icon={Facebook} />
      <IconFrame icon={Instagram} />
      <IconFrame icon={Youtube} /> */}
    </footer>
  );
}
